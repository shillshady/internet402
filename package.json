{
  "name": "internet402",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/types && npm run build -w packages/verifier -w packages/client && npm run build -w packages/middleware -w packages/agent-kit-plugin",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.5"
  }
}
